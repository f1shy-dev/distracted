{
  "private": true,
  "name": "distracted",
  "version": "0.0.0",
  "description": "Monorepo workspace for distracted.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "bun run lint",
    "lint:fix": "bun run lint:fix",
    "lint:ci": "bun run lint:ci",
    "format": "bun run format",
    "knip": "bun run knip",
    "check": "bun run check"
  }
}
