name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build and attach to release (e.g., v0.0.0-beta.1)"
        required: true
        type: string

jobs:
  submit:
    runs-on: ubuntu-latest
    environment: Deploy
    permissions:
      contents: write

    steps:
      - name: checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: cache bun
        uses: actions/cache@v4
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: install dependencies
        run: bun install --frozen-lockfile

      - name: lint
        run: bun run --cwd packages/extension lint:ci

      - name: compile
        run: bun run --cwd packages/extension compile

      - name: zip extensions
        run: |
          bun run --cwd packages/extension zip
          bun run --cwd packages/extension zip:firefox

      - name: create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag || github.ref_name }}
          files: |
            packages/extension/.output/*-chrome.zip
            packages/extension/.output/*-firefox.zip
            packages/extension/.output/*-sources.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: submit to chrome web store
        run: |
          cd packages/extension
          bunx wxt submit \
            --chrome-zip .output/*-chrome.zip --chrome-publish-target default
        env:
          CHROME_EXTENSION_ID: ${{ vars.CHROME_EXTENSION_ID }}
          CHROME_CLIENT_ID: ${{ vars.CHROME_CLIENT_ID }}
          CHROME_CLIENT_SECRET: ${{ secrets.CHROME_CLIENT_SECRET }}
          CHROME_REFRESH_TOKEN: ${{ secrets.CHROME_REFRESH_TOKEN }}

      - name: submit to firefox addons
        run: |
          cd packages/extension
          bunx wxt submit \
            --firefox-zip .output/*-firefox.zip --firefox-sources-zip .output/*-sources.zip --firefox-channel listed
        env:
          FIREFOX_EXTENSION_ID: ${{ vars.FIREFOX_EXTENSION_ID }}
          FIREFOX_JWT_ISSUER: ${{ vars.FIREFOX_JWT_ISSUER }}
          FIREFOX_JWT_SECRET: ${{ secrets.FIREFOX_JWT_SECRET }}
